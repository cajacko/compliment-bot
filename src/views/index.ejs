<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Complimnet Bot</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" integrity="sha384-rwoIResjU2yc3z8GV/NPeZWAv56rSmLldC3R/AZzGRnGxQQKnKkoFVhFQhNUwEyJ" crossorigin="anonymous">
  </head>

  <body class="container">
    <h1 class="mt-2">Compliment Bot</h1>

    <form class="mt-5" method="POST">
      <div class="form-group">
        <label for="addUser">Add a twitter user to compliment</label>
        <div class="input-group">
          <span class="input-group-addon">@</span>
          <input type="text" class="form-control" id="addUser" name="username">
        </div>
        <% if (locals.userSuccess) { %>
          <div class="alert alert-success mt-2" role="alert">
            <strong>@<%= userSuccess %></strong> successfully added
          </div>
        <% } else if (locals.userError) { %>
          <div class="alert alert-danger mt-2" role="alert">
            <%= userError %>
          </div>
        <% } %>
      </div>
      <input type="hidden" name="type" value="addUser" />
      <button type="submit" class="btn btn-default">Submit</button>
    </form>

    <form class="mt-5" method="POST">
      <div class="form-group">
        <label for="addCompliment">Add a compliment</label>
        <textarea class="form-control" rows="3" id="addCompliment" name="tweet"></textarea>
        <% if (locals.tweetSuccess) { %>
          <div class="alert alert-success mt-2" role="alert">
            <strong><%= tweetSuccess %></strong> : successfully added
          </div>
        <% } else if (locals.tweetErr) { %>
          <div class="alert alert-danger mt-2" role="alert">
            <%= tweetErr %>
          </div>
        <% } %>
        <p>Use "@user" to designate where the user handle should go.</p>
        <p>e.g.<br>What a lovely person @user is<br><br>For @charliejackson will tweet as:<br>What a lovely person @charliejackson is</p>
      </div>
      <input type="hidden" name="type" value="addTweet" />
      <button type="submit" class="btn btn-default">Submit</button>
    </form>

    <script src="https://code.jquery.com/jquery-3.1.1.slim.min.js" integrity="sha384-A7FZj7v+d/sdmMqp/nOQwliLvUsJfDHW+k9Omg/a/EheAdgtzNs3hpfag6Ed950n" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/tether/1.4.0/js/tether.min.js" integrity="sha384-DztdAPBWPRXSA/3eYEEUWrWCy7G5KFbe8fFjk5JAIxUYHKkDx6Qin1DkWx51bBrb" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/js/bootstrap.min.js" integrity="sha384-vBWWzlZJ8ea9aCX4pEW3rVHjgjt7zpkNpZk+02D9phzyeVkE+jo0ieGizqPLForn" crossorigin="anonymous"></script>
  </body>
</html>
